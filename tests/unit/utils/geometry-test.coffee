`import geometry from '../../../utils/geometry'`
`import { module, test } from 'qunit'`

module 'Unit | Utility | geometry'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = geometry()
  assert.ok result
