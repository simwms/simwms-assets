`import multimerge from '../../../utils/multimerge'`
`import { module, test } from 'qunit'`

module 'Unit | Utility | multimerge'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = multimerge()
  assert.ok result
