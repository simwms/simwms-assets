<h4>Material Card</h4>
<p>
  <a href="https://www.google.com/design/spec/components/cards.html" target="blank">
    Google's card specs can be found here
  </a>
</p>
<p>Material cards collection helps organize your cards and present them in an interesting way.</p>

<div class="container">
  {{#material-cards-collection cards=model as |card|}}
    {{#material-power-card image=card.image}}
      <div class="power-card-content">
        <div class="power-card-title">
          {{card.name}}
        </div>
        <div class="power-card-subtitle">
          {{card.style}}
        </div>
      </div>
    {{/material-power-card}}
  {{/material-cards-collection}}
</div>

<div class="container">
  {{#material-cards-collection cards=model as |card|}}
    {{#material-power-card image=card.image}}
      {{#link-to "card" class="power-card-link"}}
        <div class="power-card-link-content">
          <div class="power-card-title">
            {{card.name}}
          </div>
          <div class="power-card-subtitle">
            {{card.style}}
          </div>
        </div>
        <div class="power-card-link-icon">
          <i class="fa fa-phone fa-2x"></i>
        </div>
      {{/link-to}}
    {{/material-power-card}}
  {{/material-cards-collection}}
</div>

<div class="container">
  {{#material-cards-collection cards=model as |card|}}
    {{#material-card-link "index" image=card.image}}
      {{#link-to "card" class="power-card-link"}}
        <div class="power-card-link-content">
          <div class="power-card-title">
            {{card.name}}
          </div>
          <div class="power-card-subtitle">
            {{card.style}}
          </div>
        </div>
        <div class="power-card-link-icon">
          <i class="fa fa-phone fa-2x"></i>
        </div>
      {{/link-to}}
    {{/material-card-link}}
  {{/material-cards-collection}}
</div>